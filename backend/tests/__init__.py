"""Tests for Nutri-AI. Run from `backend/`:

    python -m unittest discover -s tests -t .

`unittest` rather than pytest so the suite runs on a bare interpreter — one less
thing to install before you can check whether the maths is right (design.md §14.3).
"""
